# 正式域名，上线后填 https://你的域名
# Sites 托管时由运行环境提供；本地默认 http://localhost:3000
SITE_URL=
NEXT_PUBLIC_SITE_URL=

# GitHub 个人访问令牌（public_repo 只读即可）
# 不填也能跑，但热门仓库、详情、预览更容易碰到额度上限
GITHUB_TOKEN=

# 手动调用刷新接口时使用；不填则 /api/cron/refresh 会拒绝
CRON_SECRET=
