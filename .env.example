# 正式域名，上线后填 https://你的域名
# 不填时，Vercel 会用项目自己的生产域名；本地默认 http://localhost:3000
NEXT_PUBLIC_SITE_URL=

# GitHub 个人访问令牌（public_repo 只读即可）
# 不填也能跑，但热门仓库、详情、预览更容易碰到额度上限
GITHUB_TOKEN=

# 每天刷新热门缓存用。Vercel Cron 会自动带这个密钥
# 不填则 /api/cron/refresh 会拒绝，首页热门不会按日作废
CRON_SECRET=
